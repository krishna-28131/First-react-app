# React.memo & Lazy Loading Demo

## Features
- Parent component with counter
- Heavy child component
- Lazy loading using React.lazy
- Optimization using React.memo only

## Expected Behavior
- Heavy component loads only once
- Counter updates do not re-render heavy component
- Console log confirms performance optimization

## Run Project
1. npm install
2. npm start
